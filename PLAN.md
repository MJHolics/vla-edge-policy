# VLA Edge Policy — 로봇 위에서 돌아가는 VLA는 무엇을 포기해야 하는가

> 착수 2026-08-18 · 측정 환경 WSL2 Ubuntu 22.04 · RTX 4080 SUPER 16GB · torch cu124
> **코드를 쓰기 전에 무엇을 잴지부터 정한다**(이 레포군의 기존 방식 — 벤치마크 먼저).

---

## ① 목표·이유 — 왜 이걸 하는가

**트렌드**: π0·GR00T·Gemini Robotics·Helix가 연달아 나오고 휴머노이드가 실제 조립 라인에 들어갔다.
VLA(Vision-Language-Action)는 2026년 embodied AI의 중심축이다.

**그런데 성공률로는 경쟁할 수 없다.** 표준 벤치마크 LIBERO의 평균 성공률은 16개월 만에
**75% → 98%** 로 올라 사실상 포화됐다(Libra-VLA 97.2%, RotVLA 98.2%). 여기서 +0.5%p를 다투는 것은
GPU를 가진 연구실의 일이고, 포트폴리오로서도 "돌려봤다"와 구분되지 않는다.

**포화되지 않은 축은 따로 있다.** 업계가 실제로 막혀 있는 지점은 이것이다 —
> VLA 추론은 **로봇 안/옆 하드웨어에서 30~100Hz로, 지연 상한이 보장된 채** 돌아야 한다.

성공률 98%짜리 정책이 5Hz로만 돈다면 로봇 위에서는 쓸 수 없다.
**그래서 이 프로젝트가 재는 것은 정확도가 아니라 "제어 주기 제약 아래에서의 정확도"다.**

이 축은 지민종의 기존 실측 자산과 정확히 맞물린다 — TensorRT 132→316FPS, ONNX INT8 엣지 CNN,
INT8이 1.38배를 얻고 검출 절반을 잃은 발견, 다중 스트림 서빙의 드롭0 상한, 임베디드 실무 2년 7개월.
**새로 배우는 것은 "정책 학습"이고, 그 위에 얹는 것은 이미 가진 "경량화·실측·검증"이다.**

## ② 상품성 — 누가 왜 쓰나

- **사용자**: 로봇/제조 자동화 회사의 온보드 추론 담당. "이 정책을 우리 컨트롤러에 올릴 수 있나"에 답해야 하는 사람.
- **가치**: 모델 선택을 **성공률 단일 지표가 아니라 (성공률, 제어 주기, 메모리) 삼중 제약**으로 바꾼다.
  "어떤 압축을 어디까지 하면 몇 Hz가 나오고 성공률을 얼마나 잃는가"를 표로 준다.
- **파이프라인 적합**: 추적 중인 로봇 계열 공고 30건 이상(네이버랩스 13·뷰런 8·케이알엠 6·씨메스 3·
  케이스랩·테솔로·에브리봇·두핸즈). 케이알엠에서 **ROS2 미보유가 "이 건의 유일한 정면 갭"**으로 기록됨 → ⑦에서 닫는다.

## ③ 무엇을 쓸 것인가 (채용 근거는 실측 후 확정)

| 축 | 후보 | 고른 이유(가설) |
|---|---|---|
| 프레임워크 | **LeRobot**(HuggingFace) | VLA·모방학습 정책이 한 인터페이스로 모여 있어 **정책 간 공정 비교**가 가능. 사실상 표준 |
| 벤치마크 | **LIBERO** (Spatial/Object/Goal/Long) | VLA 평가의 사실상 표준 → **내 수치를 공개 수치와 견줄 수 있다**. 자체 환경을 만들면 비교 대상이 없어진다 |
| 주 정책 | **SmolVLA-450M** | 소비자 GPU 대상 설계. **RTX 4080 SUPER(16GB)에서 bf16 파인튜닝 가능**이 문헌으로 확인됨 |
| 비교 정책 | π0(expert-only FT) · ACT · Diffusion Policy | VLA vs 비-VLA, 대형 vs 소형의 **크기–성능 축**을 만든다. π0 full FT는 24GB 초과라 백본 동결만 |

> 이 표는 **가설이다.** 실제 채용 근거는 ④를 측정한 뒤 확정하고, 틀렸으면 틀렸다고 기록한다.

## ④ 무엇을 측정할 것인가 — **코드를 쓰기 전에 확정**

### 지표 3축

| 축 | 지표 | 재는 법 |
|---|---|---|
| 정확도 | LIBERO suite별 성공률 | 표준 프로토콜(suite당 10태스크). 시드 3개, 평균±표준편차 |
| **지연** | 추론 1회 p50/p95 (ms) → **달성 가능 제어 Hz** | 워밍업 후 N회 반복. **CV가 50%를 넘는 지표는 배수로 인용하지 않는다**(기존 규칙) |
| 자원 | VRAM 피크, 모델 크기(MB) | `torch.cuda.max_memory_allocated` · 체크포인트 크기 |

### 제어 주기 예산은 **문헌 값이 아니라 이 임베디먼트의 값**을 쓴다 (2026-08-18 정정)

처음엔 문헌의 "VLA는 30~100Hz로 돌아야 한다"를 그대로 예산으로 잡았다. 그런데 `LiberoEnv` 설정에
답이 박혀 있었다:

```python
fps: int = 20  # Must match robosuite's default control_freq (20 Hz)
```

**LIBERO의 제어 주파수는 20Hz다.** 30~100Hz는 휴머노이드·고주파 제어 문헌의 수치이고,
이 벤치마크의 로봇은 그렇게 안 돈다. 남의 숫자를 예산으로 쓰면 결론이 이 실험과 무관해진다.

**확정 예산**

| 실행 방식 | 1회 추론이 감당해야 하는 로봇 시간 | 지연 예산 |
|---|---|---|
| 폐루프(청크 1스텝만 실행) | 1스텝 = 50ms | **≤ 50ms** |
| 청킹 H스텝 실행 | H × 50ms | **≤ 50·H ms** |
| 기본 설정(H=50) | 50스텝 = **2.5초** | ≤ 2,500ms |

이렇게 놓으면 질문이 선명해진다 — **기본값(H=50)은 예산이 2.5초라 사실상 아무 정책이나 통과한다.
그래서 진짜 질문은 "H를 얼마나 줄일 수 있는가"이고, 그건 곧 "얼마나 폐루프에 가깝게 갈 수 있는가"다.**
개루프 구간이 길수록 외란·오차에 취약해지므로, H를 줄이는 것은 성능이 아니라 **강건성**을 사는 일이다.
참고로 30Hz·100Hz 예산도 같이 보고해 다른 임베디먼트로의 전이 가능성을 남긴다.

### 핵심 파생 지표 — 이게 이 프로젝트의 결론이다

> **제어 주기 예산(30Hz=33ms · 50Hz=20ms · 100Hz=10ms) 안에 들어가는 구성 중 성공률 최대치는?**

정확도 최대가 아니라 **제약 하 최대**를 찾는다. 이 프레이밍은 `video-stream-serving`에서
"제약 위반 실험을 25%→6%로 줄인 것이 BO의 진짜 이득"이라고 결론냈던 축과 같다.

### 돌릴 손잡이 (하나씩, 한 번에 하나만)

> 아래 목록은 **`SmolVLAConfig`를 직접 열어 본 뒤 고친 것**이다(2026-08-18). 기본값을 가정하지 않고 읽었더니
> 계획에 없던 손잡이가 하나 나왔고, 있다고 생각한 손잡이 하나는 이미 켜져 있었다.

| # | 손잡이 | 기본값(실측 확인) | 왜 지연에 걸리나 | 대가 |
|---|---|---|---|---|
| 1 | **디노이징 스텝** `num_steps` | **10** | 1회 액션 예측에 **액션 전문가를 10번 통과**한다. 스텝 수에 지연이 거의 선형 | 궤적 품질 저하 → 성공률 |
| 2 | **액션 청킹** `chunk_size` / `n_action_steps` | **50 / 50** | 1회 추론으로 50스텝을 뽑아 개루프 실행. 유효 제어 Hz = 추론 Hz × 실행 스텝 수 | 개루프 드리프트 |
| 3 | **입력 해상도** `resize_imgs_with_padding` | **(512, 512)** | 비전 토큰 수 → VLM 통과 비용 | 미세 물체 인식 저하 |
| 4 | **양자화** | bf16 | 메모리 대역폭 병목 완화 | **`vlm-defect-inspector`에서 INT8이 1.38배를 얻고 검출 50.56%를 잃은 전례** — 속도만 보고하지 않는다 |
| 5 | 비전 인코더 동결 | `freeze_vision_encoder=True` · `train_expert_only=True` | — | **이미 켜져 있다.** 학습 메모리 손잡이지 추론 지연 손잡이가 아니다 |

**계획 수정 2건**

- **추가**: `num_steps`(디노이징 10회)를 **1순위 손잡이로 올린다.** 처음 계획에 없었는데, 설정을 읽어 보니
  액션 1회 예측에 전문가를 10번 통과한다. 플로우매칭 계열에서 보통 **가장 큰 단일 레버**이고
  정확도와 정면으로 맞바꾼다. `n_obs_steps=1`이라 관측 스택 비용은 없다.
- **강등**: "비전 인코더 동결"을 지연 손잡이 목록에서 뺀다. **기본값이 이미 동결**이라 여기서 얻을 게 없다.
  가정으로 목록에 넣었던 것이고, 읽어 보니 틀렸다.

**청킹 축의 실제 의미도 달라졌다.** 기본이 이미 `n_action_steps=50`이므로 질문은
"청킹을 켤 것인가"가 아니라 **"50스텝을 개루프로 밀어도 되는가, 몇에서 무너지는가"**다.
1/5/10/25/50에서 성공률과 유효 Hz를 같이 잰다.

### ⚠ 측정 구조 확정 — **성공률과 지연은 다른 곳에서 재야 한다** (2026-08-18 실측으로 확정)

정책을 붙이기 전에 환경 비용부터 쟀다(`tools/bench_env_cost.py`, 인스턴스 3회 × 60스텝).

| 구성 | env.step p50 (평균±sd) | 관측 리프 |
|---|---:|---|
| 카메라 2대(기본) | **119.8 ± 7.4 ms** | `pixels.image`, `pixels.image2` |
| 카메라 1대 | **117.4 ± 7.8 ms** | `pixels.image` |

**카메라를 실제로 절반 줄였는데 비용이 안 변했다**(차이 −2.4ms, 2σ=15.7ms 안 → 결론 보류).
env.step 비용은 렌더링이 아니라 **물리 스텝이 지배**한다.

→ **sim 종단 상한이 8~9Hz다. 어떤 정책을 붙여도 그렇다.**
   따라서 sim 종단 wall-clock으로 "제어 주기 몇 Hz"를 주장하는 것은 **정책이 아니라 시뮬레이터를 재는 것**이다.

**확정한 측정 구조**

| 무엇을 | 어디서 | 왜 |
|---|---|---|
| **성공률** | sim 롤아웃 | 스텝 수 기반이라 wall-clock과 무관하다. env가 느려도 값은 정확하다 |
| **지연** | **정책 단독**(env 없이 관측 텐서를 직접 먹임) | 이 값만이 실기로 전이된다. 실기에서는 물리가 공짜다 |
| 제어 주기 예산 판정 | 위 둘을 결합 | env 비용은 **결론에 들어가지 않는다** |

실기 카메라 캡처(30fps=33ms)는 파이프라인과 병렬로 돌므로 sim의 env 비용과 대응되지 않는다.
**sim env 비용은 이 프로젝트의 결론에서 완전히 배제하고, 배제했다는 사실을 명시한다.**

### 반드시 같이 보고할 것 (과장 방지)

- **성공률은 시드 3개 평균±표준편차.** 단일 실행 수치로 정책 순위를 매기지 않는다
  (`nano-pretrain-scaling` A3에서 single seed가 순위를 오도한 전례).
- **압축의 대가를 반드시 동반 보고.** "몇 배 빨라졌다"만 쓰지 않는다.
- **지연은 어느 구간을 쟀는지 명시.** 순수 추론인지, 전처리·후처리 포함인지
  (`video-stream-serving` I12에서 "TensorRT 2.4배"가 구간을 바꾸니 1.23배였던 전례).

## ⑤ 교체 기록

| # | 무엇을 | 무엇으로 | 근거 |
|---|---|---|---|
| 1 | 평가 `--policy.empty_cameras=1` | **0** (학습 조건) | 학습 데이터셋에 카메라가 2대뿐이고 `train_config.json`이 `empty_cameras=0`. 학습은 이미지 2장으로 이뤄졌다. A/B 64% vs 60%(구간 겹침) — **이득 때문이 아니라 학습 조건과 같아야 해서** 되돌렸다 |
| 2 | 지연 벤치가 `empty_cameras`를 코드에서 **1로 강제** | `--empty-cameras` 손잡이(기본 0) | 강제값 때문에 §2 지연 수치가 **학습에 없던 카메라를 포함해** 측정됐다. 재측정 대상 |
| 3 | 비교 기준 "공개 수치 90%대" | **"재현 가능한 상한 83%"** (lerobot issue #3287) | 논문 수치를 재현한 공개 사례가 없다. 재현 시도도 83%에서 멈췄고 이슈는 답변 없이 열려 있다. 재현된 적 없는 숫자를 기준선으로 쓰면 격차 해석이 통째로 틀어진다 |
| 4 | 1순위 가설 "카메라 3대로 학습됐는데 2대만 준다" | **폐기** | 학습 데이터셋을 직접 열어 보니 카메라가 2대뿐이었다. `config.json`의 camera3 선언은 사실이 아니다 |
| 5 | 2순위 가설 "state 표현 불일치(6 vs 8)" | **폐기** | 런타임 8차원이 전부 학습 정규화 분포 안. dim3≈π로 axis-angle까지 일치 |

## ⑥ 에러 대처 기록

환경 구축에서 물린 함정 9개는 `SETUP.md`에 있다. 여기는 **측정 단계**에서 물린 것.

| # | 증상 | 진짜 원인 | 교훈 |
|---|---|---|---|
| 1 | `FileNotFoundError: 'lerobot-eval'` | `venv/bin/python tools/x.py`로 부르면 **venv의 `bin/`이 PATH에 없다.** venv를 활성화했을 때만 우연히 동작했다 | 서브프로세스로 부르는 실행파일은 `Path(sys.executable).with_name(...)`로 **절대경로**를 잡는다 |
| 2 | `TypeError: ModelCardData is not JSON serializable` | HF `model_info().cardData`는 dict가 아니라 객체 | 메타 조회 결과를 `json.dumps`에 그냥 넣지 않는다 |
| 3 | `ValueError: Policy type 'smolvla' is not registered. Available: {}` | `PreTrainedConfig.from_pretrained`만 임포트하면 **정책 레지스트리가 비어 있다** | `lerobot.policies.factory`를 임포트해 레지스트리를 채운 뒤 설정을 연다 |
| 4 | `KeyError: 'observation.state'` | `observation.state`는 `preprocess_observation`이 아니라 **env 전처리기**(`LiberoProcessorStep`)가 `robot_state`를 합쳐 만든다 | 관측을 직접 지어내지 말고 **평가와 같은 체인**을 그대로 태운다 |
| 5 | 성공률 60% — "정책이 이상하다"로 끝낼 뻔했다 | 하네스가 학습에 없던 패딩 카메라를 넣고 있었다(수정) · 비교 기준이 재현된 적 없는 수치였다(교체) | **낮은 수치를 봤을 때 모델을 의심하기 전에 하네스와 기준선을 의심한다** |
| 6 | 조기 재추론 트리거 실행(10태스크 중 7개 완료)이 저장 직전 통째로 증발 | 절전이 아니라 **Windows Update가 WSL 스토어 앱 자체를 자동 갱신하며 실행 중이던 WSL2 VM을 재시작**시킴(이벤트 로그 `WSL Service` 재설치 시각과 정확히 일치) | 오래 도는 WSL 백그라운드 작업은 절전(§SETUP.md)뿐 아니라 **WSL/Store 자동 업데이트로도 죽을 수 있다** — 태스크 단위로 즉시 저장 + 이어서 실행 가능하게 짜는 게 근본 대책 |

## ⑦ 개발 순서

| 단계 | 내용 | 상태 |
|---|---|---|
| P0 | 환경 구축(WSL2 + LeRobot + LIBERO), GPU 학습 1스텝 통과 | 🔄 |
| P1 | **측정 하네스 먼저** — 성공률·지연·VRAM을 한 번에 뽑는 러너. 사전학습 정책으로 baseline 확보 | ⬜ |
| P2 | SmolVLA를 LIBERO 1개 suite에 파인튜닝 → 성공률 재현(공개 수치와 대조) | ⬜ |
| P3 | 손잡이 4개 스윕(청킹·양자화·인코더·해상도) → **제약 하 파레토 곡선** | 🔄 (2026-09-12: 청킹 스윕 + 파훼 시도까지 완료 — 고정 n_action_steps 1→50, 82.7%→48.7%(기본값 50에서만 구간 밖 하락). 조기 재추론 트리거(`tools/adaptive_chunk_rollout.py`) 구현·실측: 79.3%로 nas=50의 절벽은 피했으나 nas=10(76.0%) 대비 성공률 우위는 통계적으로 무의미하고 재추론 비용은 오히려 nas=10의 약 2배(임계값 과민 — 난이도 무관하게 거의 항상 nas≈5 수준으로 발동). "적응형이 고정값을 이긴다"는 가설은 이 캘리브레이션에서는 기각, 원인·수치는 `RESULTS.md` §5.2. 양자화·인코더·해상도는 여전히 ⬜) |
| P4 | ROS2 노드로 래핑 — 정책을 토픽 인터페이스 뒤에 두고 주기 지터 측정 (**케이알엠 정면 갭**) | ✅ (2026-09-02: 서비스 왕복 오버헤드 <0.5% + 20Hz 타이머 폐루프 실측(달성 2.87Hz, 6.96배 초과) — `docs/ros2_bridge.md`. 미완 잔여: 오버런 대응 정책 비교) |
| P5 | 카드·지식베이스 반영, 로봇 계열 세트에 전파 | 🔄 (K11·K12, 카드 ④·⑦·강조스위치 반영 완료 · 로보티즈·플라잎 세트는 개별 판단 필요) |
| P6 | MuJoCo 물리 파라미터 직접 조작 → 도메인 랜덤화 Sim2Real 강건성 (**플라잎·씨메스 정면 갭**) | ✅ (2026-09-03: 마찰 0.5~1.5배·질량 0.7~1.3배, 짝지음 McNemar n=150/조건 — 48.7%→52.0%, p=0.36, 이 범위에서 유의한 취약성 못 찾음. 2026-09-09: 시각 축(색상·조명) 확장 — 48.7%→62.0%, p=0.0012 유의하게 상승. 2026-09-10: 색상·밝기 분리 실험으로 원인을 색상 축으로 좁힘(색상만 +12.6%p 유의·밝기만 −1.3%p 무의미·고정색상 +4.0%p 무의미 → 무작위 다양성 자체가 필요), 태스크 언어 대조로 "물체 식별이 아니라 대비" 가설과 정합하는 태스크별 패턴 확인(2회 재현), off 기준선 자체가 완전한 결정론이 아님을 발견(48.7%↔54.0% 왕복, 재현 실험으로 확정) — `RESULTS.md` §5.1. 미완 잔여: 더 넓은 물리 랜덤화 범위·형상/카메라/질감 축·대비 단독 조작 실험) |

## 근거의 선 — 지금부터 명시해 둔다

- **시뮬레이션 전용이다.** 실물 로봇이 없으므로 sim-to-real 격차는 다루지 못한다.
  "LIBERO에서 측정했다"까지가 근거이고, 실기 성능을 주장하지 않는다.
- **지연은 RTX 4080에서 잰 값이다.** 실제 온보드 컴퓨터(Jetson 등)가 아니다.
  따라서 절대 Hz가 아니라 **구성 간 상대 비교와 압축의 대가**가 이 측정의 결론이다.
- **정책을 처음부터 학습하지 않는다.** LIBERO 사전학습 체크포인트가 공식으로 존재하므로
  (`lerobot/smolvla_libero` 23.6k dl · `lerobot/pi05_libero_finetuned_v044` 20.1k dl ·
  `pi0_libero_finetuned_v044` · `pi0fast-libero` · `xvla-libero` · `VLA-JEPA-LIBERO` 등)
  **이들을 같은 하네스로 비교하는 것**이 1차 기여다. 파인튜닝은 필요해지면 그때 한다.
  성공률 자체는 각 논문이 이미 보고했다 — 없는 것은 **같은 조건에서의 지연·제약 하 비교**다.
- **sim의 env.step 비용(≈118ms)은 결론에서 배제한다.** 시뮬레이터 오버헤드이지 정책 특성이 아니다.
